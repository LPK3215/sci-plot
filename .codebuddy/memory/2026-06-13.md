# 2026-06-13

## 架构图双语支持
- `generate_architecture.py` 重构为支持 `lang` 参数，`TEXTS` 字典包含 en/cn 双语文本
- 运行 `python docs/scripts/generate_architecture.py` 同时输出 `architecture.svg`（英文）和 `architecture_cn.svg`（中文）
- README.md 引用 `./docs/architecture.svg`（英文版），README_CN.md 引用 `./docs/architecture_cn.svg`（中文版）
- 项目结构中两个 README 均新增了 `architecture_cn.svg` 条目

## 网页版架构图更新
- project_overview.html 的 Architecture 区域从手写 HTML 流程图（~170 行）替换为 `<img src="docs/architecture_cn.svg">` 直接引用中文 SVG
- styles.css 删除了旧的 .workflow-wrap/.pipeline/.pipeline-step 等约 250 行样式，替换为 .arch-svg-wrap + .arch-svg-img
- 网页 lang=zh-CN，内容以中文为主，引用中文版 SVG

## 规则确认
- 英文文档使用英文版资源，中文文档使用中文版资源
- SKILL.md 是 AI 面向文件，绝不能包含 HTML 样式
